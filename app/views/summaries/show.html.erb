<h1><%= @summary.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @summary.body %></p>
  </div>
  <div class="col-md-4">
      <%= link_to "Edit", edit_summary_post_path(@summary, @post), class: 'btn btn-success' %>
    <% end %>
  </div>
</div>
